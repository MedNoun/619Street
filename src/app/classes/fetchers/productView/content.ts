import { navbar } from '../shared/navbar';
import { productView } from './productView';
import { carousel, carouselImage } from '../homepage/carouselImage';
export interface content {
    navbar: navbar;
    carousel: carousel;
    productView: productView;
}
